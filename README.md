# CofreSenhas

Esse aplicativo é um cofre de senhas. Tem por finalidade apenas o aprendizado e por isso disponibilizo o código aberto para outros desenvolvedores olharem. É o Primeiro sistema em CRUD que eu Fiz, fiz uma ligação com o banco de dados do tipo SqLite, que cria uma arquvico local na máquina, uma instância de um banco, utilizado a Variavel DBConnection, e colocava o comando dentro de uma strng Query e executava com a variável command.

ele possiu erros e não é sua versão final. Usei o mínimo de IA possível (Utilizei em raríssimas vezes para um melhor entendimento), então algumas partes que estiverem esquisitas eu atribuo somente a IA, que confunde os códigos e viaja tem hora kkkkkkkk. 

Esse Sistema Possui alguns erros Como: 
  Pede o caminho do banco toda vez que inicia o banco (Sinceramente, ainda não tenho ideia de como resolver isso).
  Strings anuláveis -> alguns inputs que o usuário utilizar pode quebrar completamente o sistema se estiver fora do script.
  Falta de interfaçe -> É uma aplicação em Console, não sou acostumado com front-end no momento, então se divirta com os meus Console.writeLine();
